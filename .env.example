GITHUB_TOKEN=ghp_replace_me

# Optional: default CLI args for `docker compose up` (ignored by `docker compose run`,
# which takes its args after the service name). Example:
# GETGIT_ARGS=octocat --max-prs 100
